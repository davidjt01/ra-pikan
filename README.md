# RA-PIKAN: Residual-Adaptive Physics-Informed Kolmogorov–Arnold Networks

Implementation of **RA-PIKAN**, a hybrid three-cycle training algorithm that interleaves
physics-residual diagnosis with spatially targeted B-spline grid densification
and residual-adaptive collocation augmentation (RAR-D, Wu et al. 2023).

The RA-PIKAN algorithm runs:
- **Cycles 0–1**: targeted B-spline extension, concentrating new knots in high-residual sub-domains
- **Cycle 2**: uniform grid extension across the full domain (avoids ill-conditioned least-squares projection after two targeted extensions)
- **Probe-gated training**: during each refinement cycle, 30% of the phase budget is a probe; if L² improvement is below 10%, the remaining steps are deferred to a supplementary Adam phase after all cycles

## Project structure

| Path | Purpose |
|------|---------|
| `src/` | Library: KAN/PIKAN layers, PDE residuals, benchmarks, RA-PIKAN algorithm, trainers, metrics, symbolic extraction |
| `experiments/` | Scripts to run benchmarks and generate figures/tables (see `experiments/README.md`) |
| `results/` | CSV outputs, convergence logs, PDF figures (generated by scripts) |
| `requirements.txt` | Pinned Python dependencies |

## Setup

```bash
# Python 3.9+ required
python -m venv .venv

# Windows
.venv\Scripts\activate
# Linux / macOS
source .venv/bin/activate

pip install -r requirements.txt
```

## Running experiments

### Quick smoke test

```bash
python experiments/run_all.py --quick --device cpu
```

Quick mode uses one seed (42), the smallest difficulty setting per benchmark, and four methods
(`mlp_pinn`, `fixed_pikan`, `fixed_bspline_pikan`, `ra_pikan`). It does not run
`run_sensitivity.py`. Per-run Adam/L-BFGS step budgets are unchanged (only seeds, parameters,
and methods are reduced).

### Full experiment suite

Runs all four benchmarks × six methods on CPU (five seeds each; Advection–Diffusion
uses ten seeds). Also runs `run_sensitivity.py`:

```bash
python experiments/run_all.py --device cpu
```

Already-present results are skipped automatically; the run is safe to interrupt
and resume.

### Run individual benchmarks

Each benchmark script defaults to three seeds (42, 123, 456) when `--seeds` is omitted.
Pass `--seeds` explicitly to match the full-suite seed list (see [Benchmarks](#benchmarks)).

```bash
# 1D Burgers (ν=0.01, seed=42, all methods)
python experiments/run_burgers.py --nu 0.01 --seeds 42 --device cpu

# 1D Allen-Cahn (default 2000-step Adam budget)
python experiments/run_allen_cahn.py --eps 0.1 --seeds 42 --device cpu

# 2D Advection-Diffusion
python experiments/run_advection_diffusion.py --Pe 100 --seeds 42 --device cpu

# 1D Duffing (symbolic extraction sub-study)
python experiments/run_duffing.py --seeds 42 --device cpu
```

### Generate all figures and tables

```bash
python experiments/make_figures_and_tables.py
```

Writes CSV summaries and PDF figures to `results/`. Per-parameter figures use the benchmark's
parameter values (see [Benchmarks](#benchmarks)); residual maps also include a `{method}`
suffix (`ra_pikan` or `uniform_pikan`).

#### Tabular outputs

| File | Contents |
|------|----------|
| `table_accuracy.csv` | Mean ± std L² error by method, benchmark, and parameter |
| `table_compute.csv` | Elapsed time, steps/s, peak memory |
| `table_symbolic.csv` | Extracted formulas and R² (Duffing) |

#### Summary figures

One PDF per benchmark.

| File | Contents |
|------|----------|
| `fig_bar_burgers.pdf` | Final L² error by method (all ν) |
| `fig_bar_allen_cahn.pdf` | Final L² error by method (all ε) |
| `fig_bar_adv_diff.pdf` | Final L² error by method (all Pe) |

#### Per-parameter figures

| Pattern | ν / ε / Pe values | Contents |
|---------|-------------------|----------|
| `fig_convergence_burgers_nu{ν}.pdf` | 0.01, 0.005, 0.001 | Training loss vs. Adam step |
| `fig_convergence_ac_eps{ε}.pdf` | 0.1, 0.05 | Training loss vs. Adam step |
| `fig_convergence_ad_Pe{Pe}.pdf` | 100.0, 500.0 | Training loss vs. Adam step |
| `fig_l2cycle_burgers_nu{ν}.pdf` | 0.01, 0.005, 0.001 | L² error vs. refinement cycle |
| `fig_l2cycle_ac_eps{ε}.pdf` | 0.1, 0.05 | L² error vs. refinement cycle |
| `fig_l2cycle_ad_Pe{Pe}.pdf` | 100.0, 500.0 | L² error vs. refinement cycle |
| `fig_solution_burgers_nu{ν}.pdf` | 0.01, 0.005, 0.001 | Predicted vs. reference field |
| `fig_solution_ac_eps{ε}.pdf` | 0.1, 0.05 | Predicted vs. reference field |
| `fig_solution_adv_diff_Pe{Pe}.pdf` | 100, 500 | Predicted vs. reference field (2D); e.g. `fig_solution_adv_diff_Pe100.pdf` |

Adv-Diff Pe is written as `100.0` / `500.0` in convergence, L²-cycle, and residual-map
filenames; solution plots omit the decimal (`Pe100`, `Pe500`).

#### Residual maps

Spatial PDE residual per refinement cycle.

| Pattern | Generated for |
|---------|---------------|
| `fig_residuals_burgers_nu{ν}_{method}.pdf` | ν ∈ {0.01, 0.001}; `ra_pikan`, `uniform_pikan` |
| `fig_residuals_ac_eps{ε}_ra_pikan.pdf` | ε ∈ {0.1, 0.05}; RA-PIKAN only |
| `fig_residuals_adv_diff_Pe{Pe}_{method}.pdf` | Pe ∈ {100.0, 500.0}; `ra_pikan`, `uniform_pikan` |

#### Analysis figures

| File | Contents |
|------|----------|
| `fig_gini_evolution.pdf` | Gini coefficient across refinement cycles |
| `fig_kan_activations_duffing.pdf` | KAN edge activations (Duffing, seed 42) |
| `fig_duffing_symbolic.pdf` | Symbolic extraction R² distribution |
| `fig_sensitivity_percentile.pdf` | RA-PIKAN sensitivity to residual percentile threshold (requires `run_sensitivity.py`; data in `results/sensitivity/`) |

## Methods

| Method | Key file | Description |
|--------|----------|-------------|
| MLP-PINN | `src/models.py` | Tanh MLP — [2,64,64,64,1] for Burgers/Allen-Cahn/Adv-Diff, [1,64,64,1] for Duffing |
| Fixed ChebyPIKAN | `src/models.py` | Chebyshev KAN — [2,10,10,1] / [1,10,10,1], degree 5, no grid refinement |
| Fixed B-spline PIKAN | `src/models.py` | B-spline KAN — same widths, grid=5, no grid extension (ablation baseline) |
| RAD-PINN | `experiments/run_*.py` | MLP + residual-adaptive collocation augmentation (RAR-D; Wu et al. 2023) |
| Uniform-ext PIKAN | `src/ra_pikan.py` | B-spline KAN + global uniform grid extension each cycle (Duffing: inline loop in `experiments/run_duffing.py`) |
| **RA-PIKAN** | `src/ra_pikan.py` | B-spline KAN + 2 targeted cycles + 1 uniform cycle + probe-gated training |

## Benchmarks

| Benchmark | Parameters | Seeds (full `run_all.py`) | Challenge |
|-----------|-----------|---------------------------|-----------|
| 1D Burgers | ν ∈ {0.01, 0.005, 0.001} | 5 | Sharp shock front |
| 1D Allen–Cahn | ε ∈ {0.1, 0.05} | 5 | Stiff phase interface |
| 2D Advection–Diffusion | Pe ∈ {100, 500} | 10‡ | Exponential boundary layer |
| 1D Duffing | δ=0.5, α=1.0, β=0.1, γ=0.3, ω=1.2 | 5 | Nonlinear ODE; symbolic extraction |

Seeds for Burgers, Allen–Cahn, and Duffing: 42, 123, 456, 789, 1234.
‡Advection–Diffusion adds 2024, 3141, 4096, 5678, 6789 (10 seeds total).

## Step budget (equal across all methods, per Wu et al. 2023)

| Benchmark | Adam steps | L-BFGS steps | RA-PIKAN / Uniform-PIKAN phase split |
|-----------|-----------|-------------|---------------------------------------|
| Burgers / Allen-Cahn | 2000 | 100 | 500 base + 3×500 refinement + 100 L-BFGS |
| Adv-Diff | 3000 | 200 | 750 base + 3×750 refinement + 200 L-BFGS |
| Duffing (MLP, fixed ChebyPIKAN, fixed B-spline) | 3000 | 200 | flat Adam, then L-BFGS |
| Duffing (RAD-PINN, uniform-ext PIKAN) | 3000 | 200 | 750 base + 3×750 refinement + 200 L-BFGS |
| Duffing (RA-PIKAN) | 3000 | 200 | 1000 base + 2×1000 refinement + 200 L-BFGS† |

†Duffing RA-PIKAN and uniform-ext PIKAN use custom loops in `experiments/run_duffing.py`
rather than `ra_pikan_train()` / `uniform_extension_train()`. RA-PIKAN uses ChebyKAN with
2 refinement cycles; uniform-ext uses BSplinePIKAN with 3 cycles. The symbolic extraction
sub-study targets the Chebyshev basis.

## Key results

Mean L² relative error over five seeds (42, 123, 456, 789, 1234), aggregated from the
CSV files in `results/`. A full `run_all.py` reproduction uses ten seeds for
Advection–Diffusion; the tables below reflect the five-seed results currently present.

### Allen-Cahn (ε=0.1 and ε=0.05)

| Method | ε=0.1 | ε=0.05 |
|--------|-------|--------|
| MLP-PINN | 8.71e-02 | 1.22e-01 |
| RAD-PINN | 9.69e-02 | 1.34e-01 |
| Fixed B-spline PIKAN | 1.39e-02 | 1.14e-02 |
| Fixed ChebyPIKAN | 6.22e-03 | 1.01e-02 |
| Uniform-ext PIKAN | 6.85e-04 | 1.23e-03 |
| **RA-PIKAN** | **4.99e-04** | **4.75e-04** |

RA-PIKAN beats Uniform-ext PIKAN by **1.37×** (ε=0.1) and **2.58×** (ε=0.05).

### Advection-Diffusion

| Method | Pe=100 | Pe=500 |
|--------|--------|--------|
| MLP-PINN | 3.69e+00 | 4.97e+00 |
| RAD-PINN | 3.03e+00 | 3.07e+00 |
| Fixed ChebyPIKAN | 4.61e+00 | 4.57e+00 |
| Fixed B-spline PIKAN | 3.11e+00 | 3.11e+00 |
| Uniform-ext PIKAN | 2.98e+00 | 3.08e+00 |
| **RA-PIKAN** | **1.76e+00** | 3.09e+00 |

RA-PIKAN is best for Pe=100 (1.69× over Uniform-ext). At Pe=500 all methods produce L²≈3 — the boundary layer (~0.002) is below collocation resolution for all methods.

### Burgers

All methods yield high L² errors at low viscosity due to shock sharpness. Fixed ChebyPIKAN outperforms RA-PIKAN here (global Chebyshev basis better suited than local B-spline refinement for sub-grid shock fronts).

| Method | ν=0.01 | ν=0.005 | ν=0.001 |
|--------|--------|---------|---------|
| Fixed ChebyPIKAN | **5.25e-02** | **1.49e-01** | 3.39e-01 |
| MLP-PINN | 1.34e-01 | 2.85e-01 | 3.63e-01 |
| RAD-PINN | 1.35e-01 | 2.71e-01 | 3.51e-01 |
| RA-PIKAN | 1.84e-01 | 3.18e-01 | 3.55e-01 |
| Uniform-ext PIKAN | 2.21e-01 | 2.88e-01 | **2.69e-01** |
| Fixed B-spline PIKAN | 3.02e-01 | 3.19e-01 | 3.52e-01 |

## Reproducibility

- Each run fixes `torch.manual_seed(seed)` and `numpy.random.seed(seed)`; RAR-D resampling also uses `numpy.random.default_rng(seed)`.
- Hyperparameters are defined as constants / dataclass fields at the top of each script.
- Summary metrics: `{benchmark}_{method}_{param}_seed{seed}.csv` (e.g. `burgers_ra_pikan_nu0.01_seed42.csv`). Auxiliary logs use prefixes such as `{benchmark}_convergence_*`, `{benchmark}_l2history_*`, `{benchmark}_ginihistory_*`.
- `make_figures_and_tables.py` reads only from `results/` — no re-running needed.
